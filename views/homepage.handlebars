{{#each posts as |post| }}
{{> post-summaries}} {{!-- this will come from a partials file --}}
{{/each}}

{{!-- {{#each posts as |post| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h1>this is the homepage</h1>
    <h2><a href="../models/Post.js">{{post.title}}</a></h2>
  </div>
  <div class="col-md-7">
    <p>
      {{post.body}}
    </p>
  </div>
</div>
{{/each}} --}}