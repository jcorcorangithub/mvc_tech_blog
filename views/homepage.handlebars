{{#each posts as |post| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h1>this is the homepage</h1>
    <h2><a href="../models/Post.js">{{post.title}}</a></h2>
    {{!-- <p>Created by {{project.user.name}} on {{format_date project.date_created}}</p> --}}
  </div>
  <div class="col-md-7">
    <p>
      {{post.body}}
    </p>
  </div>
</div>
{{/each}}